# Approved script remediation — September 2026

This change addresses the findings from the 7–8 September reviews. It is a repository patch, not a production deployment. Earlier static review covered 59 scripts across two script repositories; it was not a full runtime certification. Unchanged scripts remain outside this patch's runtime claims.

## Finding map and migration requirements

| Finding | Updated implementation | Operator migration / remaining validation |
| --- | --- | --- |
| C1, H1: DiskCleanup | Default inventory of old `.tmp` files in explicit allowlisted directories; volume roots/reparse points refused. Execute requires an exact reviewed CSV digest and unchanged file metadata/hash. Every deletion uses ShouldProcess and LiteralPath. Broad document/backup deletion, event clearing, crash-evidence removal and DISM ResetBase removed. | Previous options are replaced. Use protected, trusted directories; inventory, review and pin a manifest. Validate representative application temp-file lifecycle. No automatic rollback after deletion. |
| H6: CA backup | Versioned destinations; supported ADCSAdministration API; SecureString password; all CA logs and prior backups retained; registry exit code checked; completion marker after artifact checks. | Dot-source function; provide existing protected parent. `-Force` no longer overwrites. `-Extended` is unsupported. Lab full/incremental/key restore is mandatory before relying on recovery; incremental chains require their full base and subsequent increments. |
| H7: compromised users | Explicit immutable user plan and expected tenant; per-action ShouldProcess; 24-character cryptographic password; no secret logging; secure handoff callback required for reset. Inbox/forwarding evidence is exported before mutation. | Existing connected Graph/Exchange sessions and protected EvidenceDirectory required. Actions are explicit integers 1–8; old interactive menu removed. No automatic module installation. Tenant permission/denied-action/secure delivery tests remain mandatory. Password resets, session revocation and mailbox actions are disruptive. |
| H8: AD health | Collection errors distinguished from empty success; UNKNOWN blocks green aggregate status. Event queries distinguish no matches from access/RPC errors. Async native output draining has a timeout. Repeated OS queries cached. | Windows/AD tests remain required. Native diagnostic text is supporting evidence, not proof of health. |
| H10: tasks | Real collection, strict parsing of every action, explicit result state; empty/failed/malformed scopes cannot report clean. COM and script actions need review. | Heuristics are indicators, not malware clearance. Validate expected task scope on each server. |
| H11: environment report | UNKNOWN/ERROR affects overall status. Replication uses structured AD cmdlet output. Event levels collected together. Backup verification explicitly UNKNOWN/unsupported. | Configure an authoritative backup-product check before expecting an overall green report. No backup timestamp is fabricated. |
| H12: firewall monitoring | Deterministic normalized snapshots with associated application/port/address/service/interface filters, SHA256, before/after diffs and bounded repeated failures. | Snapshot audit is not actor attribution or exhaustive firewall-policy auditing. Correlate operational/security events. Pre-provision protected log directory. Validate rule/filter behavior on Windows. |
| H13: group management | Every mutation helper gates on ShouldProcess. Moves and failed deletions restore original accidental-deletion protection by immutable GUID in finally. PSCredential replaces plaintext password. Bulk execution requires opt-in plus reviewed CSV SHA256. Module auto-install removed. | First use `-WhatIf`; execute with `-ExecuteBulk -InputCsvSha256 <Digest>` only after reviewing the full input. A digest pins reviewed bytes; it is not a digital signature. Group rename/property/ACL changes are not a multi-operation transaction; stop and inspect partial failures. |
| M2: AD backup | Explicit drive target, preinstalled feature, synchronous wbadmin execution, immediate exit-code and catalog retrieval checks. | Drive-letter targets only. No LogPath/AlertAgeDays promises. Verify the newly created catalog version and restore in a lab. |
| M2: mailbox conversion | Mandatory CSV (`UPN` header), expected tenant GUID, full input validation and immutable mailbox identity; ShouldProcess and post-conversion check. | Existing Exchange Online session required. Validate licensing, archive/hold and rollback requirements in the target tenant. |
| M3: password expiry | Actual directory mail, per-user computed expiry and sentinel handling, accurate future/expired distinction, default preview and explicit SMTP send, same-day dedup markers. | `-Server` required. Sending requires explicit SMTP/from/state configuration. SMTP acceptance/marker interruption can still duplicate delivery; no exactly-once claim. |
| M4: Entra devices | Explicit existing tenant/session/scopes, paged managed devices, indexed serial lookup, optional relationship/BitLocker metadata, per-record partial state, single final CSV publication. | Use `-IncludeRelationships` and `-IncludeBitLockerPresence` when needed. Key presence is not proof that encryption is active. App-only roles and target-tenant compatibility require validation. |
| M5: risky users | One canonical implementation, optional paged history, buffered CSV publication. Duplicate file fails explicitly without issuing queries. | Migrate all scheduled consumers to `Get_Risky_User-Report.ps1 -TenantId <TenantId>` and schedule it once. `-IncludeHistory` opts into extra API calls. |

## Isolated validation

`tests/Validate-Scripts.ps1` parses all PowerShell sources (including extensionless files), loads function definitions without running their operational entry points, and tests task collection failure, UNKNOWN health states, password generation/expiry, firewall diffing, AD move protection restoration and local cleanup fixtures. It contains no real tenant, AD, CA, firewall, email or backup operation.

The GitHub workflow runs Windows PowerShell 5.1 and PowerShell 7 on disposable Windows runners without tenant credentials. Passing these checks cannot replace application-aware backup restoration, PKI recovery, Windows module integration or production access tests. The change does not claim a measured speedup; indexing, optional lookups and combined event queries reduce avoidable work.

## Rollback and deployment

Before deployment, record the currently deployed revision, scheduled task arguments, execution identity, target scope and recovery requirements. Stage the scripts in a separate directory and run previews in a lab. Change the scheduled entry point only through a separately approved rollout. Roll back the script deployment by returning to the previous pinned revision and arguments; do not restore unsafe cleanup/backup behavior into unattended schedules. Actual directory/mailbox/credential changes require their own operation-specific recovery plan.

Review-derived scripts include community contributions; their original sources and attribution remain in repository history. This patch supersedes older README claims where the older documentation described unimplemented parameters or safeguards.

Windows CI also identified UTF-8-without-BOM parsing failures in the existing GetUsersLogonLogoffEvents and AD_Monitoring sources under Windows PowerShell 5.1. Those files, and touched Unicode PowerShell files, now use UTF-8 with BOM; the two additional sources received encoding-only changes.
